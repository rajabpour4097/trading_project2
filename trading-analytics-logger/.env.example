import pandas as pd

# ایجاد یک DataFrame خالی
data = []

# در هر بار باز و بسته شدن معامله، اطلاعات را به data اضافه کنید
data.append({
    'timestamp': datetime.now(),
    'trade_type': 'buy',  # یا 'sell'
    'volume': 0.01,
    'entry_price': 1.2000,
    'exit_price': 1.2050,
    'profit': 50.0,
    'market_condition': 'bullish',
    'indicator_value': 70  # مثال برای RSI
})

# تبدیل به DataFrame
df = pd.DataFrame(data)

# ذخیره‌سازی در فایل CSV
df.to_csv('trading_data.csv', index=False)